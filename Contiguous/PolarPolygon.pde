/*
class PolarPolygon {
  Edge[] edges; = new Edge[numPoints];
  PVector origin;
  ArrayList tempPoints = new ArrayList();
  PolarPolygon() {
    PVector origin = new PVector(0, 0);
  }
  
  PolarPolygon(PVector pt, Edge firstEdge) {
    origin = pt;
    edges[0] = firstEdge;
  }
  
  void create() {
    float circleQuantity = TWO_PI;
    float[] radiusBounds = float[5, 20];
    float[] angleBounds = float[5, 170];
    //ArrayList tempPoints = new ArrayList();
    // if no origin points are given,
    // make 'em up
    
    if (edges[0] == null) {
      for(int i = 0; i < numPoints * 3; i++) {
        /*
        float rad = random(radiusBounds[0], radiusBounds[1]);
        float angle = radians(random(angleBounds[0], angleBounds[1]));
        tempPoints.add(new PVector(rad, angle));
        
      }
    }
    
    // do hull shrinking operation
  }
}

*/
